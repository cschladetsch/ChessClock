#!/bin/bash

resources=../ChessClock/Resources/
config=main.json

prefix=.
if [ "$OSTYPE" == "msys" ]; then
	prefix=./Debug
fi

if [ "$1" == "test" ]; then
	$prefix/GambitTest $2 $3 $4
	exit $?
fi

$prefix/ChessClock $resources $config

