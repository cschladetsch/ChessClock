#!/bin/bash

resources=../ChessClock/Resources/
prefix=.
if [ "$OSTYPE" == "msys" ]; then
	prefix=./Debug
fi

if [ "$1" == "test" ]; then
	$prefix/GambitTest $2 $3 $4
	exit $?
fi

$prefix/Gambit $resources

